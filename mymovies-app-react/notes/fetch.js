// import axios from "axios";

/*
URL => endpoint atau alamat dari API
DATA/BODY => Data yang akan dikirimkan ke API
CONFIG -> konfigurasi untuk mengirimkan ke API

//UNIVERSAL
var config = {
  method: 'get'/'post'/'put'/'delete',
  url: 'URL',
  headers: {
    'Content-Type': 'application/json', //ngasih info data yang dikirimkan adalah JSON
    'Authorization': 'Bearer ' + token, //JWT
  }
};

axios(config)

//cara  get dengan axios
axios.get(URL, CONFIG)

//POST
biasa untuk mengirim data ke server (login, register, create post, create, comment, etc)
axios.post(URL, DATA, CONFIG)

//PUT
biasa digunakan untuk mengubah data record yang sudah ada di server(update post, update comment, update profile, etc)
axios.put(URL, DATA, CONFIG)

//DELETE
biasa digunakan untuk menghapus data record yang sudah ada di server
axios.delete(URL, CONFIG)
*/
